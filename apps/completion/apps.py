from django.apps import AppConfig


class CompletionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.completion"
    verbose_name = "Completion and compliance"
